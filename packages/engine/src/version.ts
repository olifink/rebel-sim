/** Rebel Forth's own version identity — major.milestone, bumped by hand
 * alongside PLAN.md's own M-numbered milestone log, this repo's primary
 * "how far along" signal already, so VERSION (primitives.ts) reuses it
 * instead of keeping a second, separately-maintained counter. The third,
 * auto-generated build-stamp segment lives in build-info.ts instead —
 * never hand-edited, see that file's own note.
 */
export const VERSION_MAJOR = 0;
export const VERSION_MILESTONE = 49;
